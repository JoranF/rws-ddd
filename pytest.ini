[pytest]
addopts = -q
testpaths =
    beheer/tests
    contract/tests
    monitoring/tests
    onderhoud/tests
pythonpath =
    beheer
markers =
    unit: domain/application or bounded-context scaffold checks
    interface: REST, DTO, env, and published-language contract checks
    integration: cross-layer, persistence, event, and context-contract checks
